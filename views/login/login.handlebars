<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login Page</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
    <style>
        .login, .register {
            background-color: #9E2A2B;
            border: none;
            color: white;
        }

        /* Style the container */
        .container {
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
        }

        /* Style the form */
        .login-form {
            background-color: #fff;
            padding: 50px;
            border-radius: 5px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.2);
        }

        /* Add rounded corners to login and register buttons */
        .login.rounded, .register.rounded {
            border-radius: 20px;
        }
        body {
            background-color: #f0f0f0; /* Change this to your desired grey color */
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="login-form">
            <h1>Login</h1>
            <form method="POST" action="/">
                <label for="username">Username:</label>
                <input type="text" id="username" name="username">
                <br>

                <label for="password">Password:</label>
                <input type="password" id="password" name="password">
                <br>
                <span id="error-message" style="color: red;">{{errorMessage}}</span>
                <br>
                <button class="login rounded" type="submit">Login</button>
            </form>
            <p>Don't have an account? <a href="/register"><button class="register rounded">Register</button></a></p>
        </div>
    </div>
</body>
</html>
